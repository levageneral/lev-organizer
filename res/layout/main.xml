<?xml version="1.0" encoding="utf-8"?>
<ScrollView
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/scroll"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="@android:color/darker_gray">
    <LinearLayout
        android:orientation="vertical"
        android:layout_width="match_parent"
        android:layout_height="match_parent">
        <LinearLayout
            android:layout_width="match_parent"
            android:layout_height="match_parent">
            <Button
                style="@style/Button"
                android:text="@string/btn_add_task"
                android:id="@+id/btnGoTask"
                android:layout_weight="1"/>
            <Button
                style="@style/Button"
                android:id="@+id/btnGoSchedule"
                android:text="@string/btn_add_schedule"
                android:layout_weight="1"/>
        </LinearLayout>
        <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="match_parent">
            <Button
                    style="@style/Button"
                    android:id="@+id/btnShowListTask"
                    android:text="@string/btn_show_list_task"
                    android:layout_weight="1"/>
            <Button
                    style="@style/Button"
                    android:id="@+id/btnShowListSchedule"
                    android:text="@string/btn_show_list_schedule"
                    android:layout_weight="1"/>
        </LinearLayout>

    </LinearLayout>

</ScrollView>